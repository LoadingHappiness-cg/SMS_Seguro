plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
    id 'org.jetbrains.kotlin.plugin.serialization'
}

def isSignedReleaseTaskRequested =
    gradle.startParameter.taskNames.any { taskName ->
        def lowerTaskName = taskName.toLowerCase()
        lowerTaskName.contains('release') && !lowerTaskName.contains('unsigned') && !lowerTaskName.contains('debug')
    }

def releaseSigningProp = { String key ->
    return providers.gradleProperty(key).orNull
}

def releaseStoreFilePath = releaseSigningProp('RELEASE_STORE_FILE')
def releaseStorePassword = releaseSigningProp('RELEASE_STORE_PASSWORD')
def releaseKeyAlias = releaseSigningProp('RELEASE_KEY_ALIAS')
def releaseKeyPassword = releaseSigningProp('RELEASE_KEY_PASSWORD')

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

kotlin {
    jvmToolchain(17)
}

def hasReleaseSigningConfig =
    [releaseStoreFilePath, releaseStorePassword, releaseKeyAlias, releaseKeyPassword].every { value ->
        value != null && !value.trim().isEmpty()
    }

def releaseSigningErrorMessage =
    "Missing release signing configuration. Please define RELEASE_STORE_FILE, " +
        "RELEASE_STORE_PASSWORD, RELEASE_KEY_ALIAS, and RELEASE_KEY_PASSWORD in ~/.gradle/gradle.properties"

android {
    namespace 'com.smsguard'
    compileSdk 35

    defaultConfig {
        applicationId "com.smsguard"
        minSdk 28
        targetSdk 35
        versionCode 1
        versionName "1.0.0"

        buildConfigField "String", "RULESET_JSON_URL", "\"https://raw.githubusercontent.com/LoadingHappiness-cg/SMS_Seguro/main/rules/ruleset-latest.json\""
        buildConfigField "String", "RULESET_SIG_URL", "\"https://raw.githubusercontent.com/LoadingHappiness-cg/SMS_Seguro/main/rules/ruleset-latest.sig\""

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        vectorDrawables {
            useSupportLibrary true
        }
    }

    signingConfigs {
        release {
            if (hasReleaseSigningConfig) {
                storeFile file(releaseStoreFilePath)
                storePassword releaseStorePassword
                keyAlias releaseKeyAlias
                keyPassword releaseKeyPassword
            } else if (isSignedReleaseTaskRequested) {
                throw new GradleException(releaseSigningErrorMessage)
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            if (hasReleaseSigningConfig) {
                signingConfig signingConfigs.release
            }
        }
        releaseUnsigned {
            initWith release
            signingConfig null
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
    buildFeatures {
        compose true
        buildConfig true
    }
    composeOptions {
        kotlinCompilerExtensionVersion '1.5.8'
    }
    packagingOptions {
        resources {
            excludes += '/META-INF/{AL2.0,LGPL2.1}'
        }
    }
    lint {
        abortOnError true
        warningsAsErrors false
        checkDependencies true
    }

    applicationVariants.all { variant ->
        variant.outputs.all {
            outputFileName = "sms-seguro-${variant.versionName}.apk"
        }
    }
}

tasks.configureEach { task ->
    if ((task.name == 'assembleRelease' || task.name == 'bundleRelease') && !hasReleaseSigningConfig) {
        task.doFirst {
            throw new GradleException(releaseSigningErrorMessage)
        }
    }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.7.0'
    implementation 'androidx.activity:activity-compose:1.8.2'
    implementation platform('androidx.compose:compose-bom:2024.02.00')
    implementation 'androidx.compose.ui:ui'
    implementation 'androidx.compose.ui:ui-graphics'
    implementation 'androidx.compose.ui:ui-tooling-preview'
    implementation 'androidx.compose.material3:material3'
    
    // Allowed Dependencies
    implementation "androidx.work:work-runtime-ktx:2.9.0"
    implementation "com.squareup.okhttp3:okhttp:4.12.0"
    implementation "org.jetbrains.kotlinx:kotlinx-serialization-json:1.6.2"
    
    // Icons
    implementation "androidx.compose.material:material-icons-extended"

    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
    androidTestImplementation platform('androidx.compose:compose-bom:2024.02.00')
    androidTestImplementation 'androidx.compose.ui:ui-test-junit4'
    debugImplementation 'androidx.compose.ui:ui-tooling'
    debugImplementation 'androidx.compose.ui:ui-test-manifest'
}
